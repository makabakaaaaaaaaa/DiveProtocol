namespace DiveProtocol
{
    /// <summary>High-level application states for the empty game flow.</summary>
    public enum GameState
    {
        Boot,
        MainMenu,
        Loading,
        InRun,
        Results
    }
}
