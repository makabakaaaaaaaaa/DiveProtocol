namespace DiveProtocol.Builds
{
    /// <summary>Run progression stage that supplies a fixed build selection node.</summary>
    public enum BuildSelectionTier
    {
        Core,
        Reinforcement,
        Utility,
        Awakening
    }
}
